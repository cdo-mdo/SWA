server:
  port: 8081

spring:
  application:
    name: ServiceA
  config:
    import: "optional:configserver:http://localhost:8888"

management:
  endpoints:
    web:
      exposure:
        include: health,info
